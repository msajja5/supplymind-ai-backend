{
  "name": "supplymind-ai-backend",
  "version": "1.0.0",
  "description": "SupplyMind AI — Real-time data pipeline backend for India→EU aerospace supply chain",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step — serverless functions'",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "lint": "eslint api/ lib/"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.x"
  },
  "license": "MIT"
}
